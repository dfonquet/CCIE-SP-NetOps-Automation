Purpose:
  Stores customer-facing service change definitions.

What belongs here:
  One YAML file per customer, VRF, lab exercise, or CRQ.

How it is used:
  The render workflow reads these files to generate per-device IOS XR
  candidate configs.

What you can add next:
  Create new customer YAML files for new L3VPN, multicast, or inter-AS
  services.
