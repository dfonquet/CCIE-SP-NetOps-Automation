Purpose:
  Stores Python utilities used by the offline and render workflows.

What belongs here:
  Parsers, validators, renderers, topology builders, and helper scripts.

How it is used:
  Run these scripts locally or from CI to parse configs, validate data, render
  templates, and verify generated configs.

What you can add next:
  Add JSON Schema validation, graph checks, config diff generation, or
  inventory generation from the live lab.
