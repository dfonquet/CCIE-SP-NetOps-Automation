Purpose:
  Stores CI/CD examples for external systems such as GitLab CI.

What belongs here:
  Pipeline examples, runner notes, protected branch rules, deployment gates,
  and CI/CD reference files.

How it is used:
  Use these files as references when building a real pipeline for your lab.

What you can add next:
  Add production runner tags, Slack/email notifications, protected
  environments, or CRQ integration jobs.
