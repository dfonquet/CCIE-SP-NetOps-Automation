Purpose:
  Stores pyATS/Genie validation files.

What belongs here:
  Testbed files, aetest scripts, parser-based checks, and service validation
  logic.

How it is used:
  pyATS runs after deployment to validate ISIS, BGP, SR, VPNv4, VPNv6, and
  service health.

What you can add next:
  Add Genie parser checks, per-VRF assertions, SR policy validation, and
  structured test reports.
