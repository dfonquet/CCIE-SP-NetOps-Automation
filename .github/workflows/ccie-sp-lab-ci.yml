name: CCIE SP Lab CI

"on":
  pull_request:
    paths:
      - "automation/**"
      - "full-configs/**"
  push:
    branches:
      - main
    paths:
      - "automation/**"
      - "full-configs/**"
  workflow_dispatch:

jobs:
  offline-vision:
    name: Offline Vision
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build lab facts
        run: python automation/scripts/build_lab_facts.py

      - name: Validate lab facts
        run: python automation/scripts/validate_lab_facts.py

      - name: Upload generated topology
        uses: actions/upload-artifact@v4
        with:
          name: ccie-sp-lab-vision
          path: automation/generated/
